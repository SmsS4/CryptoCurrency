package com.example.homework1;

public enum TimeStart {
    /**
     * Pass as param to CoinIoApi.
     * Week: returns candles of one week
     * MONTH: returns candles of one month
     * */
    WEEK,
    MONTH
}
